(a b) ((lambda x. x) y);
(lambda x. x) (a b);
lambda x. ((lambda x. x) ((lambda x. x) (lambda z. ((lambda x. x) z))));
lambda x. x;
(lambda x. x) (lambda x. x x); 
x ((lambda x. x) y);
lambda x. ((lambda y. (lambda x. x z)) x);
(lambda x. x) (lambda x. x x); 