(executable
 (name main)
 (modules_without_implementation syntax))

(ocamllex lexer)

(ocamlyacc parser)

(env
 (dev
  (flags
   (:standard -unsafe-string)))
 (release
  (flags
   (:standard -unsafe-string))))
